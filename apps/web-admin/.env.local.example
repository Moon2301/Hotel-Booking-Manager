# Leave empty to call /api/v1 on the Next.js app (recommended for local dev)
NEXT_PUBLIC_API_URL=

# NestJS base URL (server-side BFF + rewrites); paths append /api/v1 automatically
API_INTERNAL_URL=http://localhost:3000
